[pytest]
# Scope collection to tests/ only. tools/test_*.py are standalone validation
# SCRIPTS (torch / model artifacts / network) and must not be collected by a
# bare `pytest` run.
testpaths = tests
addopts = -q
